package ipg.cooling.calc;

/** Straight runs of the serpentine relative to the rectangular plate. */
public enum TubeOrientation {
    /** Straights follow the longer plate side (height). */
    ALONG_HEIGHT,
    /** Straights follow the shorter plate side (width). */
    ALONG_WIDTH
}
